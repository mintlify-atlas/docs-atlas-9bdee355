---
title: "Quickstart"
description: "Get your wedding website up and running in under 5 minutes"
---

<Info>
  A live demo is available at [wedding.rampatra.com](http://wedding.rampatra.com/). Use invite code `271117` to try the RSVP form.
</Info>

## Prerequisites

Before you begin, make sure you have the following installed:

- [Git](https://git-scm.com/) — to clone the repository
- [Node.js](https://nodejs.org/) (v14 or later) — includes npm
- [Gulp CLI](https://gulpjs.com/docs/en/getting-started/quick-start) — to run the build pipeline

Install the Gulp CLI globally if you haven't already:

```bash
npm install --global gulp-cli
```

## Setup

<Steps>
  <Step title="Clone the repository">
    Clone the Wedding Website project to your local machine:

    ```bash
    git clone https://github.com/rampatra/wedding-website.git
    ```
  </Step>

  <Step title="Navigate to the project directory">
    ```bash
    cd wedding-website
    ```
  </Step>

  <Step title="Install dependencies">
    Install all npm dependencies (Gulp, Sass, animate.css, Font Awesome, Waypoints):

    ```bash
    npm install
    ```

    This installs both `devDependencies` (gulp, gulp-sass, gulp-uglify, gulp-rename, sass) and `dependencies` (animate.css, font-awesome, waypoints).
  </Step>

  <Step title="Build the project">
    Run the default Gulp task to compile SCSS to CSS and minify JavaScript:

    ```bash
    gulp
    ```

    The `default` task runs two subtasks in sequence:

    | Task | What it does | Output |
    |---|---|---|
    | `sass` | Compiles `sass/styles.scss` → compressed CSS | `css/styles.min.css` |
    | `minify-js` | Minifies `js/scripts.js` | `js/scripts.min.js` |

    You can also run individual tasks:

    <CodeGroup>
      ```bash Compile SCSS only
      gulp sass
      ```

      ```bash Minify JS only
      gulp minify-js
      ```

      ```bash Watch SCSS for changes (Gulp 3 syntax — may not work with Gulp 4)
      gulp sass:watch
      ```
    </CodeGroup>
  </Step>

  <Step title="Open the website">
    Double-click `index.html` to open it in your browser, or use a local server:

    ```bash
    npx serve .
    ```

    <Tip>
      You should see the full wedding website with a hero section, navigation, "How we met" story, events, engagement photos, map, and RSVP form. All animations and sections should load correctly.
    </Tip>
  </Step>
</Steps>

## What's next

Now that the website is running locally, personalize it for your wedding:

<CardGroup cols={2}>
  <Card title="Couple details" icon="heart" href="/customization/couple-details">
    Update the couple's names, your love story, and invitation text.
  </Card>
  <Card title="RSVP setup" icon="envelope" href="/customization/rsvp-google-sheets">
    Connect the RSVP form to your own Google Sheet.
  </Card>
  <Card title="Events" icon="calendar" href="/customization/events">
    Add your wedding ceremony and reception details.
  </Card>
  <Card title="Deploy free" icon="cloud" href="/deployment/github-pages">
    Push your personalized site live on GitHub Pages.
  </Card>
</CardGroup>
